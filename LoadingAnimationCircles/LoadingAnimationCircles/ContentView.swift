 
import SwiftUI

struct ContentView: View {
    var body: some View {
        HStack(spacing:100){
            DotSpinnertow()
            DotSpinnerView()
        }
    }
}

#Preview {
    ContentView()
}
